import { SergioDirectiveDirective } from './sergio-directive.directive';

describe('SergioDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SergioDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
